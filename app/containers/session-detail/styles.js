import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  reply: {
    justifyContent:'center',
    alignItems:'center',
    height: 40,
    lineHeight: 40,
    borderTopWidth: 1,
    borderColor:'#efefef'
  }
})
